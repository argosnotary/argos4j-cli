---
kind: pipeline
type: docker
name: argos-cli-build

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    image: argosnotary/argos-build:3.6.3
    commands:
      - mvn -q install
    volumes:
      - name: mvn_cache
        path: /root/.m2/repository


